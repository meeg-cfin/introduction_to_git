"""
This is simple python script to use as a introduction to git version control
system. 

It contains several errors that will be corrected over the introduction.

@author: Mads Jensen
"""
import numpy as nmp

numbers = 10

random_numbers = np.random.randn(numbers)

print random_numbers


mean_rnb_numbers = np.mean(random_numbers)   
sd_rnd_numbers = np.std(random_numbers)
